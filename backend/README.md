# Backend

This folder is reserved for future backend services such as API routes, workers, database access, or authentication adapters.

The current application is a frontend-only Next.js app that integrates directly with Liveblocks from `frontend/`.
